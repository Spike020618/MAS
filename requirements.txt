# ═══════════════════════════════════════════════════════════
# 博弈驱动的去中心化语义机制 - 依赖清单
# ═══════════════════════════════════════════════════════════

# ── 核心 Web 框架（Agent 节点通信用）──────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
pydantic>=2.0.0

# ── 数据处理 ─────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0

# ── 中文分词（词级 TF-IDF 和 BM25 的前置依赖）────────────
jieba>=0.42.1

# ── 语义算法：BM25（v2 新增，替代字符级方案）─────────────
#    conda: conda install -c conda-forge rank_bm25
rank-bm25>=0.2.2

# ── MinHash / SimHash（混合语义引擎）────────────────────
datasketch>=1.6.0
simhash>=2.1.0

# ── 图论（共识网络拓扑分析）──────────────────────────────
networkx>=3.0

# ── 实验可视化 ───────────────────────────────────────────
matplotlib>=3.7.0
scipy>=1.11.0           # t 检验显著性分析

# ═══════════════════════════════════════════════════════════
# 可选：Sentence-BERT 语义嵌入（精度最高的非 LLM 方法）
# 安装：pip install sentence-transformers
# 模型将在首次运行时自动下载（~80MB）
# ═══════════════════════════════════════════════════════════
sentence-transformers>=2.2.0

# ═══════════════════════════════════════════════════════════
# 可选：LLM-as-Judge（需要 API Key，见 mas/config.py）
# 任何兼容 OpenAI Chat API 的服务均可使用，无需额外安装包
# ═══════════════════════════════════════════════════════════
# openai>=1.0.0   # 若使用 openai 官方 SDK（可选）

# ═══════════════════════════════════════════════════════════
# 可选：GPU 加速（CUDA 环境）
# ═══════════════════════════════════════════════════════════
# torch>=2.0.0   # 由 sentence-transformers 自动管理

# ═══════════════════════════════════════════════════════════
# RAG 系统（任务分配向量数据库 + 工作流）
# ═══════════════════════════════════════════════════════════
pymilvus>=2.5.0          # Milvus 客户端（向量数据库）
minio>=7.2.0             # MinIO 客户端（对象存储）
etcd3>=0.13.0            # Etcd 客户端（分布式配置）
langgraph>=0.0.69        # LangGraph（AI 工作流框架）
langchain>=0.1.0         # LangChain（LLM 集成）
faiss-cpu>=1.7.4         # FAISS（向量相似度搜索）
