# ═══════════════════════════════════════════════════════════
# 生产级 RAG 系统 - 依赖清单
# Production-grade RAG System - Requirements
# ═══════════════════════════════════════════════════════════

# ── 向量数据库 ─────────────────────────────────────────────
pymilvus>=2.5.0          # Milvus 客户端
minio>=7.2.0             # MinIO 对象存储（Milvus 依赖）
etcd3>=0.13.0            # Etcd 客户端（Milvus 依赖）

# ── HTTP 客户端（DashScope API 调用）──────────────────────
httpx>=0.24.0            # 异步 HTTP 客户端

# ── 数据处理 ─────────────────────────────────────────────
numpy>=1.24.0            # 数值计算

# ── 可选：本地向量化（SentenceTransformers）───────────────
# 如果不使用 DashScope API，可以用本地模型
sentence-transformers>=2.2.0

# ═══════════════════════════════════════════════════════════
# 备注
# ═══════════════════════════════════════════════════════════
# 
# 核心依赖：pymilvus, httpx, numpy
# 
# 可选：sentence-transformers 用于离线向量化
#   安装：pip install sentence-transformers
#   首次运行时会自动下载模型（~80MB）
#
# ═══════════════════════════════════════════════════════════
